Euler's so-called totient function phi(m) is defined as the number of positive integers r (1 <= r < m) that are coprime to m. Two numbers are coprime if their greatest common divisor equals 1.
Example: m = 10: r = 1,3,7,9; thus phi(m) = 4.
